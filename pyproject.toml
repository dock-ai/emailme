[project]
name = "dock-ai-emailme"
version = "0.1.0"
description = "Simple EmailMe MCP server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "resend>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
